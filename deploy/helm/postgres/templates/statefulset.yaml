apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: patom-postgres
  annotations:
    argocd.argoproj.io/sync-wave: "-1"   # after its ExternalSecret (-2), before the app (0)
  labels:
    {{- include "postgres.labels" . | nindent 4 }}
spec:
  serviceName: {{ .Values.service.name }}
  replicas: 1
  selector:
    matchLabels:
      {{- include "postgres.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "postgres.selectorLabels" . | nindent 8 }}
    spec:
      securityContext:
        fsGroup: 999            # postgres uid/gid in the official image
      containers:
        - name: postgres
          image: {{ .Values.image }}
          # No readOnlyRootFilesystem: Postgres needs a writable data dir.
          securityContext:
            runAsNonRoot: true
            runAsUser: 999
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            seccompProfile:
              type: RuntimeDefault
          # Leading "-" args make the entrypoint exec `postgres -c ...`.
          args:
            - "-c"
            - "shared_buffers={{ .Values.config.sharedBuffers }}"
            - "-c"
            - "max_connections={{ .Values.config.maxConnections }}"
            - "-c"
            - "work_mem={{ .Values.config.workMem }}"
          env:
            - name: POSTGRES_USER
              value: {{ .Values.user | quote }}
            - name: POSTGRES_DB
              value: {{ .Values.database | quote }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: patom-postgres
                  key: POSTGRES_PASSWORD
            # Keep data in a subdir so initdb isn't confused by lost+found on the volume.
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          ports:
            - name: postgres
              containerPort: {{ .Values.service.port }}
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", {{ .Values.user | quote }}, "-d", {{ .Values.database | quote }}]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", {{ .Values.user | quote }}, "-d", {{ .Values.database | quote }}]
            initialDelaySeconds: 30
            periodSeconds: 15
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: {{ .Values.persistence.storageClass }}
        resources:
          requests:
            storage: {{ .Values.persistence.size }}
