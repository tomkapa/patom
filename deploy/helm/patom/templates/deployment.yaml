apiVersion: apps/v1
kind: Deployment
metadata:
  name: patom
  annotations:
    # stakater/reloader watches the envFrom secretRef (patom-config) and
    # rolling-restarts this Deployment whenever its contents change — so an
    # OpenBao secret rotation goes live without a manual `kubectl rollout
    # restart`. No-op if the reloader controller isn't installed.
    reloader.stakater.com/auto: "true"
  labels:
    {{- include "patom.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  # Single 4 GB node: avoid two app pods (2× RAM) overlapping during a rollout.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "patom.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "patom.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: patom
      automountServiceAccountToken: false   # app doesn't call the k8s API
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: patom
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
          # Non-secret config from values; secret config from the ESO-synced Secret.
          env:
            {{- range $k, $v := .Values.env }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
            # Origin-derived config, keyed off the single customer domain
            # (ingress.host) so it cannot drift. PATOM_OAUTH_REDIRECT_BASE is
            # required by the app; PATOM_WEB_BASE_URL = same origin is harmless.
            - name: PATOM_OAUTH_REDIRECT_BASE
              value: {{ include "patom.publicUrl" . | quote }}
            - name: PATOM_WEB_BASE_URL
              value: {{ include "patom.publicUrl" . | quote }}
            {{- with .Values.app.cookieDomain }}
            # Opt-in: cross-subdomain cookie Domain. Empty (default) → host-only.
            - name: PATOM_COOKIE_DOMAIN
              value: {{ . | quote }}
            {{- end }}
            {{- with .Values.app.corsAllowedOrigins }}
            # Opt-in: extra credentialed CORS origins. Empty (default) → no CORS.
            - name: PATOM_CORS_ALLOWED_ORIGINS
              value: {{ join "," . | quote }}
            {{- end }}
            {{- if .Values.sandbox.enabled }}
            # #218: point the app at the in-cluster sandbox executor. Its presence
            # is what makes `run_code` register (alongside object storage); unset
            # ⇒ the tool is absent (fail-closed).
            - name: PATOM_SANDBOX_EXECUTOR_URL
              value: "http://patom-sandbox-executor:{{ .Values.sandbox.service.port }}"
            {{- end }}
          envFrom:
            - secretRef:
                name: patom-config
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          # Listener binds only AFTER 103 migrations run; until then the port
          # refuses connections, so startupProbe must be generous.
          startupProbe:
            httpGet:
              path: /healthz
              port: http
            failureThreshold: {{ .Values.probes.startup.failureThreshold }}
            periodSeconds: {{ .Values.probes.startup.periodSeconds }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
            failureThreshold: {{ .Values.probes.liveness.failureThreshold }}
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
            failureThreshold: {{ .Values.probes.readiness.failureThreshold }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            # readOnlyRootFilesystem: give the process a writable scratch dir
            # (otel/sqlx may buffer to a temp path).
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
